notes:
  connection:
    mode: LOCAL
    server: eumail/iba
    username: ${NOTES_USERNAME:}
    password: ${NOTES_PASSWORD:}
    pool-size: 5
    timeout-ms: 30000
  tools:
    delete-enabled: true

spring:
  ai:
    mcp:
      server:
        name: hcl-notes-mcp
        version: 1.0.0
        type: SYNC
