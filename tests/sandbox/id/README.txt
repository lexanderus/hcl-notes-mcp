Place a password-free Notes ID file here as: sandbox.id

The ID must be created without a password so that tests can call
NotesFactory.createSession("", "", "") — Initialize("") — without user interaction.

This file is gitignored (binary, contains private key material).
